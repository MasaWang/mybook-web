{
  "name": "oceanai-web-book",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:manuscript": "node scripts/sync-manuscript.mjs",
    "dev": "npm run sync:manuscript && astro dev",
    "build": "npm run sync:manuscript && astro check && astro build",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.5.1",
    "astro": "^5.13.5",
    "marked": "^16.2.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.9.2"
  }
}
